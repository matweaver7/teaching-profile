import * as React from "react";
import Router from "./Router";

/* TODO:
- Remove dependencies emotion and react-emotion from package.json after @csod/table
  removes them as peer dependencies and changes to using xstyled.
*/



export default (): JSX.Element => (
    <Router />
);